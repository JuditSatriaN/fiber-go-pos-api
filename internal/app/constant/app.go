package constant

// Default constants (for global setting like static template, app name, etc.)

const AppName = "fiber-go-pos-api"

// Source constants

const WebSource = "web"

// Default Constanta

const DefaultPage = 1
const DefaultLimit = 10
const DefaultStoreID = 1
const DefaultPort = ":8080"
